<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raut.swapnil.resume_builder">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity" />
        <activity
            android:name=".Education"
            android:parentActivityName=".List" />
        <activity
            android:name=".Personal_Details"
            android:parentActivityName=".List" />
        <activity
            android:name=".Experience"
            android:parentActivityName=".List" />
        <activity
            android:name=".Skills_Awards"
            android:parentActivityName=".List" />
        <activity android:name=".List"
            android:theme="@style/StartPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Project" />
        <activity android:name=".Curricular_Activities" />
        <activity android:name=".Display_PDF_List"></activity>
    </application>

</manifest>